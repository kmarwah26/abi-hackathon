"""Backend package for the ABI Supply-Chain Assistant Databricks App.

FastAPI server that exposes the Genie, Knowledge Assistant, demand-forecast, and
Lakebase functionality as a JSON API for the React frontend. Everything runs as
the app's service principal (see server.clients)."""
